In the main html file "index.html" <form action="http://localhost:3000/api/contact" method="POST"> is connected to the node.js server which makes it possible to recieve emails through the form on the website.
Therefore the server must be active for this form to actually work, i did not committ the node.js files as they, especially the .env contain information which is confidential.
